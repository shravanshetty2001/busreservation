import { AddBustimetableDto } from './add-bustimetable-dto';

describe('AddBustimetableDto', () => {
  it('should create an instance', () => {
    expect(new AddBustimetableDto()).toBeTruthy();
  });
});
